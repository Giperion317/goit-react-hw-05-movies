import { ColorRing } from 'react-loader-spinner';
import { ColorLoader } from './Loader.styled';

export const Loader = () => {
    return (
        <ColorLoader>
            <ColorRing />
        </ColorLoader>
)
}

